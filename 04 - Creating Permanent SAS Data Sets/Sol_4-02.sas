/*
 * 4-2
Run PROC PRINT on the data set you created in Problem 1. Use the SAS
VIEWTABLE window to open this data set and compare the headings in the
window to the column headings from your PROC PRINT. 
 */

proc print data=learn.perm noobs;
run;
